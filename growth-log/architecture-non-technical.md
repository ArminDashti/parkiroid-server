# Architecture (non-technical)

The app runs as three Docker containers that talk to each other on a shared network. Helper scripts build the app image and start or update that stack on this machine or a remote server.
