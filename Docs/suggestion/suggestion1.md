# Suggestion: Rename Docker Compose project

Compose still uses the legacy project name `parkiroid-server` (container names like `parkiroid-server-dogan-server-1`). Set `COMPOSE_PROJECT_NAME=dogan` in `.env` or rename the repo folder for cleaner container names.
